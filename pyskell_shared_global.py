variables = []
variables_inputs = []
version = "1.0.0"
DEV_MODE = False

command_options = {
  'without_print_flag': '--skip-init',  # Don't print the output of the file build.
  'no_assign': '--no-assign'            # Don't print the assignments
}

execution_config = {
  'assignations_print': True,
}